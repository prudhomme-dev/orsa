{% extends 'base.html.twig' %}

{% block title %}Changer son mot de passe - {{ name_site }}{% endblock %}

{% block body %}

    <h2 class="row justify-content-center p-2">Réinitialisation de mot de passe</h2>

    <div class="row justify-content-around">
    <div class="col-12 col-md-6 align-self-center">
        <img src="{{ asset('/assets/img/reset-password-img.jpg') }}" class="img-fluid title-image" alt="S'enregistrer">
    </div>
    <div class="col-12 col-md-6 align-self-center">
        <div class="row justify-content-center">
            <div class="col">
                {{ form_start(resetForm) }}
                {{ form_row(resetForm.plainPassword) }}
            </div>
        </div>
        <div class="row justify-content-center mt-3">
            <button class="btn btn-success col-3">Valider</button>
            {{ form_end(resetForm) }}
        </div>
    </div>

{% endblock %}